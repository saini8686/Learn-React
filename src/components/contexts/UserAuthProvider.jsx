// import { db } from "../lib/firebase-config";

// import { collection, doc, setDoc } from "firebase/firestore";
// import { createContext, useContext, useEffect, useState } from "react";
// const loignformRef = (db, "login");
// class LoginFormData {
//   addUsername = (newUser) => {
//     return addDoc(loignformRef, newUser);
//   };
//   updateUser = (id, updatedUser) => {
//     const userDoc = doc(db, "login", id);
//     return updateUser(userDoc, updatedUser);
//   };
// }
// export default new LoginFormData();
